$(function() {
  $('.owl-carousel').owlCarousel({
    loop: true,
    dots: true,
    autoplay: true,
    center: true,
    autoplayTimeout: 3000,
    // // stagePadding: 20,
    // // margin: 0,
    autoWidth: true,
    autoHeight: true,
    //items: 1,
    }
  );
  // $('#hamburger').on ('click', function() {
  //   $('#nav-contain-test').slideToggle();
  // });
});
