$(function() {
  $('.owl-carousel').owlCarousel({
    loop: true,
    dots: false,
    autoplay: true,
    center: true,
    autoplayTimeout: 1000,
    // stagePadding: 20,
    // margin: 0,
    autoWidth: true,
    autoHeight: true,
    items: 1.2
    }
  );
  // $('#hamburger').on ('click', function() {
  //   $('#nav-contain-test').slideToggle();
  // });
});
